@using Entities
@using Business

@inject NavigationManager NavManager

<EditForm Model="@objCategory">
    <DataAnnotationsValidator />
    <div class="form-group">
        <label for="categoryId">Referencia:</label>
        <InputText id="categoryId" @bind-Value="objCategory.CategoryId" class="form-control" />
        <ValidationMessage For="@(() => objCategory.CategoryId)" class="text-danger" />
    </div>
    <div class="form-group">
        <label for="name">Nombre de la categoría:</label>
        <InputText id="name" @bind-Value="objCategory.CategoryName" class="form-control" />
        <ValidationMessage For="@(() => objCategory.CategoryName)" class="text-danger" />
    </div>
    <div class="form-group">
        <input type="button" value="Agregar Categoría" @onclick="Save" class="form-control btn btn-dark" />
    </div>
</EditForm>

@code {
    CategoryEntity objCategory = new CategoryEntity();

    private void Save()
    {
        CategoryBusiness.CreateCategory(objCategory);
        NavManager.NavigateTo("category/list");
    }

    private void HandleValidSubmit()
    {
        Console.WriteLine("OnValidSubmit");
    }
}
